---
status: open
tags: [D2.1, ex-2.1.1, representations]
---
# Probe all positions in a sample of sequences

So far we have only probed specific locations, e.g. last token of first operand; last token before answer. How do the other tokens compare? Visualize probe response as sublines; note that we could show multiple probes per subline (as separate series).
