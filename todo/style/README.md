# Style todo

Text and visual improvements to reports and figures: prose conventions, report structure, and figure styling. The conventions themselves live in the `style-*`, `writing` and `text-lint` skills — this is the backlog of changes not yet made.

The schema and the `./go todo` query are described once, in [`todo/README.md`](../README.md).
