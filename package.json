{
  "devDependencies": {
    "lint-staged": "^17"
  },
  "lint-staged": {
    "*.py": [
      "uv run --no-sync marimo check --fix --quiet --ignore-scripts",
      "uv run --no-sync ruff check --fix --force-exclude",
      "uv run --no-sync ruff format --force-exclude"
    ]
  }
}
